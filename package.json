{
  "name": "screenshare",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "dev:tui": "cd packages/tui && npx tsx src/index.ts",
    "dev:gui": "tauri dev",
    "build:tui": "cd packages/tui && npm run build",
    "build:gui": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
